{
  "name": "core.play_sound",
  "description": "Play a sound file via the CORE audio subsystem.",
  "type": "core",
  "parameters": [
    {
      "name": "file",
      "type": "string",
      "description": "Path to the sound file to play (relative to assets or absolute).",
      "required": true
    },
    {
      "name": "volume",
      "type": "number",
      "description": "Playback volume from 0.0 (silent) to 1.0 (max).",
      "required": false
    }
  ],
  "timeout_ms": 5000,
  "retry_limit": 0,
  "enabled": true
}

